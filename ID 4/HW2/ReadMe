первая таблица называется title вторая tabl2
cl.json выводит список всех значений (title) в обеих таблицах без повторений
rt.json выводи список всех значений (title) в обеих таблицах с повторениями
r_statistical.json выводит MIN MAX AVG SUM значений из таблицы tabl2 ключ(price) и таблицы title ключ(views) т.к. единственным повторяющимся ключом был "title"
